projection;
strict ( 2 );
use draft;

define behavior for ZAP_C_CUSTOMER
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}